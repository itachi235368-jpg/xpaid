/**
 * Audio Engine (No-op for clean UI)
 */
export const playFartSound = (_style?: string) => {
  // Silent in clean Tipped UI
};

export const fartSound = {
  isMuted: true,
  playFart: () => {},
};
